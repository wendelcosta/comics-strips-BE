service: get-comic-strips

provider:
  name: aws
  runtime: nodejs12.x

functions:
  comics:
    handler: handler.comics
    events:
      - http:
          path: /comics
          method: get

plugins:
  - serverless-plugin-typescript
  - serverless-offline
  - serverless-jest-plugin
